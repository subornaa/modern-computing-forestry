# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# Quarto
/.quarto/
/_book/
/_freeze/

# Rendered output (regenerated by `quarto render`; published via CI)
/docs/
# Generated PDF — kept locally, not tracked
/Modern_Data_Analytics_for_Forestry_and_Natural_Resources.pdf

# Rendered chapter scratch (Quarto manages these)
*_files/
*_cache/

# Analysis outputs produced during render (regenerated, not source)
/outputs/

# OS
.DS_Store
Thumbs.db

**/*.quarto_ipynb

# Private planning and draft materials (kept local, not published)
OER grant/
Draft_chapters/
Previous_materials/
plan/
transition_matrix_files/

# Instructor solutions / answer keys — kept local, distributed via Canvas only
*_solutions.qmd
*_solutions.xlsx

# TA answer-key bundle (solutions + data, organized by chapter) — never publish
/TA_solutions/

# New lab assignment set (Set B) — student starters, TA solutions, data, Canvas package — keep local
/Lab_assignments_SetB/

# Private project handoff doc — inventories confidential materials; keep local
/HANDOFF.md

# Working syllabus drafts (Round 2, v4/v5, transition matrix) — keep local, post to Canvas.
# (Three older syllabus files were committed before this rule and stay tracked.)
/syllabus/

# Per-file HTML render artifacts that leak next to .qmd sources
# (the real site is built to docs/ by CI; these are not source)
/frontmatter/*.html
/chapters/*.html
/index.html

# Demo-lab solution SOURCES (private); rendered HTML is published to lab-solutions/
/workbooks/demo-lab-solutions/
